将架构重构方案 v2 转为可操作的工程计划，从阶段 0 开始执行：

## 阶段 0：engine 纯函数补单测
1. 创建 `entry/src/ohosTest/` 目录结构
2. 配置 ohosTest target（build-profile.json5 已有声明，需实际创建测试代码）
3. 从 docs/mdcalc-validation-report.md 提取已验证的测试用例值
4. 为 P0 修复过的计算器补 hypium 单测：CPIS、CHA₂DS₂-VASc、RIPASA、Wells、Marshall、KCC、CLIF-C、Mehran
5. 逐步覆盖其他引擎函数
6. 验证：devecocli build BUILD SUCCESSFUL + ohosTest 可运行

## 阶段 1：消除 if-else 路由重复
1. 新建 `components/CalcRouter.ets`（@Component struct + @Prop calcId）
2. 从 Index.ets 迁移 88 个计算器组件 import
3. if-else 链放在 CalcRouter 的 build() 里
4. Index.ets 的 CalcModeView 和 CalcSheetBuilder 都替换为 CalcRouter({ calcId: ... })
5. 验证：devecocli build + 手动抽查 3-5 个计算器的重置功能（resetKey 重建行为验证）

## 阶段 2：拆分 AssistantService + 注册表合并 + 总线封装
1. 新建 model/FieldSchema.ets（DATA_FIELDS + FIELD_KEY_ALIASES）
2. 新建 services/CalcDispatcher.ets（callEngine 策略表 Record<calcId, fn>）
3. CalculatorRegistry 接口加 aliases + inputSpec + compute 复活
4. CALC_ID_ALIASES 并入注册表，启动时派生反向 Map 缓存
5. CALC_INPUT_SPECS 并入注册表
6. 新建 services/SheetResultBus.ets（封装 AppStorage 裸键）
7. 新建 services/prompts/ 目录，按用途分文件
8. 改造 AssistantService.ets（删迁移内容，保留对话管理+LLM提取）
9. 验证：devecocli build + AI助手功能正常

## 阶段 4：枚举化
1. 新建 model/Enums.ets（TabIndex + CalcMode）
2. Index.ets 替换魔法数字/字符串
3. EmbeddingService 加预留注释
4. 验证：devecocli build

## 阶段 3：拆分 KnowledgeService + 统一 LLM 解析
1. 新建 services/KnowledgeIndex.ets（DB + DDL + Markdown摄取）
2. 新建 services/KnowledgeSearch.ets（FTS + LLM增强检索）
3. KnowledgeService 改为门面
4. PatientService 的 LLM 解析改用 extractJsonPayload
5. 验证：devecocli build + 知识库搜索正常

## 阶段 5：UI 美学与交互体验
1. color.json headerBg 改手术青 + headerText 改 onPrimary
2. 新增 AppColors.overlay token 统一遮罩
3. 补齐 SVG 图标集替换 emoji
4. 新建 toast 工具
5. 错误横幅自动消失
6. bindSheet 按输入项数量分档高度
7. 验证：devecocli build + 逐像素核对

每阶段一个 git 分支，完成后合并。