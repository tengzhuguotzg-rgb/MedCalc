/**
 * ClarifyPrompt.ets
 *
 * 槽位文件：LlmService.buildClarifySystemPrompt() 当前的 prompt 构建
 * 仍然保留在 LlmService.ets 内部，本文件仅作为 prompts 目录的占位入口。
 *
 * 后续若将多轮卡片式澄清推荐的 prompt 提取到独立模块，可在此文件中
 * 导出 buildClarifySystemPrompt() 并将其从 LlmService 中解耦。
 */

/**
 * 占位函数（暂无实现）：用于占位以便上层 import 路径稳定。
 */
export function placeholder(): void {
}
